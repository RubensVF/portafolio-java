# Portafolio con Spring Boot, Docker.


# Requerimientos.
    -Make
    -Docker
    -Java 17
    -Maven

# Primero construir el package con 
`mvn install`

# Luego construir la imagen de docker con Make

`make build`

# Iniciar

`make start`